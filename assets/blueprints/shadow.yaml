name: shadow
shaders:
  - stage: vertex
    path: shadow.vert

rasterization:
  cull_mode: back

topology: triangle-list

depth_stencil:
  depth_test: true
  depth_write: true
  format: d32_sfloat
  compare_op: greater
